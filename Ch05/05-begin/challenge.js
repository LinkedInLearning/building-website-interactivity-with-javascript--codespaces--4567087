'use strict';

window.addEventListener('DOMContentLoaded', () => {
  // Step 1: Define the custom element
  //
  // Step 2: Add change event handlers to the quantity and price fields
  //         that will update the subtotal field
  // Hint: they refer to each other, so they can probably use the same event handler
  //
  // Step 3: Format the price and subtotal as currency ($0.00)
});
